version: "3.9"
services:
  web:
    build: .
    env_file:
      - .env
    container_name: kanmail
    ports:
      - "4420:4420"
      - "4424:8080"
    volumes:
      - data:/home/kanmail/
    restart: always
volumes:
  data: {}
